# EXM-Tweaks

## How to use? <br>
- Ask any AI tool to convert the json into a cmd or .ps1 script

## Prompt <br>
```Create a cmd or .ps1 script from the attached json file's `commands` and `revert` properties and output two separate scripts [APPLY] {tweakName}.cmd/.ps1 and [REVERT] {tweakName}.cmd/.ps1 respectively```
